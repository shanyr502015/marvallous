r = range(5)
print(r) # range(0, 5)
print(list(r)) # [0, 1, 2, 3, 4]
    # range(5) creates range object, not list.
    # print(r) shows range description, not values.
    # list(r) converts range into list of numbers.
    # range(5) starts from 0 & ends at 4.